# Quote Studio · Supabase 配置（复制本文件为 .env 后填写）
# 获取位置：Supabase 后台 → Project Settings → API
#   VITE_SUPABASE_URL        = 项目 URL（形如 https://xxxx.supabase.co）
#   VITE_SUPABASE_ANON_KEY   = anon public key（Project API keys 里的 publishable key）
#
# 不填 / 不配置时，应用自动回退到浏览器 localStorage（单机模式）。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
