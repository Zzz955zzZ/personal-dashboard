/**
 * AI 链接模块公开 API（无独立路由，仅服务与组件）。
 */
export { useAIService } from './store/ai-service';
export { aiLinkRoutes } from './routes';
export { DEFAULT_PROXY_URL } from './constants';
export type { AiRecognizedProduct } from './types';
